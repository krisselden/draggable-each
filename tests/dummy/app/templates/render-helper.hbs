<p>
  This example uses a render helper in the item template of the
  draggable-each, which originally did not work.
</p>

<div style="width: 800px; overflow: hidden; float: left">
  view: {{view.elementId}}
  {{view.childViews.length}} <br> {{view.childViews}}

  {{#draggable-each context=items handleSelector=".draggable-item-handle" itemWasMoved="itemWasMoved" connectWith=".ember-drag-list"}}
    {{render 'render-helper-item' this}}
  {{/draggable-each}}
</div>
{{#view Ember.View}}
  viewId: {{view.elementId}}
  {{view.childViews.length}} <br> {{view.childViews}}
  {{#draggable-each context=otherItems handleSelector=".draggable-item-handle" itemWasMoved="itemWasMoved" itemView="apple" connectWith=".ember-drag-list"}}
  {{/draggable-each}}
{{/view}}

